development:
  dialect: postgres
  datasource: host=localhost dbname=code-micro-videos user=postgres password=postgres sslmode=disable

production:
  dialect: postgres
  datasource: host=localhost dbname=code-micro-videos user=postgres password=postgres sslmode=disable